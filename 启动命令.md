# 启动 indexTTS2 @ wsl

启动 IndexTTS2 WebUI 服务的命令:
uv run webui.py

启动后访问 http://127.0.0.1:7860

如果你有 22GB 显存 (RTX 2080 Ti), 可以使用优化脚本 (FP16 半精度，显存减半):
./run_optimized.sh

其他常用选项:

FP16 低显存模式
uv run webui.py --fp16 --port 7860

查看所有可用选项
uv run webui.py -h
